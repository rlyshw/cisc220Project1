#include <iostream>
using namespace std;


class BigInt{
    public:
        //BigInt operator+(const BigInt &intA, const BigInt &intB){ return this*};
        //BigInt operator-(const BigInt &intA, const BigInt &intB){ return this*};
        //BigInt operator*(const BigInt &intA, const BigInt &intB){ return this*};
        //BigInt operator/(const BigInt &intA, const BigInt &intB){ return this*};
        BigInt& operator=(const BigInt &intA);
};
